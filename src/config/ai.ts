// Central AI provider config for all AI-powered services

export const DEFAULT_AI_PROVIDER = 'deepseek';

// You can change the AI provider here (e.g., ‘deepseek’, ‘openai’, ‘gemini’).
// All functions should be configured to use this provider.
